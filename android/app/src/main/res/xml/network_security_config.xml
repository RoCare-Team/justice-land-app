<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic (https://www.justiceland.online) stays TLS-only.

  The exception below exists so a debug run pointed at API_BASE_URL of
  http://10.0.2.2:3000 works against a local `next dev`, which serves plain
  HTTP. Add your machine's LAN address here if you test on a physical device.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
